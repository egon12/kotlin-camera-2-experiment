package org.egon12.renderscripttutorial

import android.renderscript.RenderScript
import androidx.lifecycle.ViewModel

class RsInputViewModel : ViewModel() {

    fun inject(renderScript: RenderScript) {

    }
}
